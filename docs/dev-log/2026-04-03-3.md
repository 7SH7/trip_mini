# 개발 일지 — 2026-04-03 (Day 1, Session 3)

## 이번 세션 작업 내역

### Tier 1: 실행 에러 수정 (4건)
- [x] `/api/users/me` 엔드포인트 추가
- [x] eureka-server / api-gateway JPA 충돌 해결 (`excludeName` 방식)
- [x] 기존 4개 서비스 DB URL에 `createDatabaseIfNotExist=true` 추가
- [x] MinIO 버킷 자동 생성 (`@PostConstruct initBucket()`)

### Tier 2: 핵심 기능 동작 (5건)
- [x] OAuth2 로그인 버튼 → 구글/카카오 redirect URL 구현
- [x] 포트원 SDK 프론트 연동 (`usePortone.ts` + iamport.js 동적 로드)
- [x] 채팅 WebSocket STOMP 실제 연결 (`@stomp/stompjs` + `sockjs-client`)
- [x] HLS 비디오 플레이어 (`hls.js` + `HlsPlayer` 컴포넌트)
- [x] FFmpeg 트랜스코딩 Kafka Consumer (`VideoUploadConsumer`)

### Tier 3: 완성도 (6건)
- [x] QueryDSL 동적 쿼리 — Trip/Booking/Payment 검색 + `/api/trips/search` 엔드포인트
- [x] MyBatis 매퍼 XML — User 통계, Payment 일별 매출, Booking 인기 여행
- [x] AES-256 암호화 — `Aes256Encryptor` + `EncryptedStringConverter` → User email 자동 암복호화
- [x] Dockerfile 전체 — 13개 서비스 + 프론트엔드 (media-service에 FFmpeg 포함)
- [x] gRPC 확장 — subscription-service 크레딧 체크/사용 gRPC 서버
- [x] 프론트 페이지 MUI+ReactQuery 전환 — TripDetail, Bookings, Payments

### 빌드 상태
- 백엔드 13개 모듈: **전체 빌드 성공**
- 프론트엔드: **빌드 성공**

---

## 남은 작업
- [ ] WebRTC 영상통화 구현 (대규모 작업)
- [ ] TDD 테스트 작성 (보류)
- [ ] 실제 실행 테스트 (docker-compose → 서비스 기동 → E2E 확인)
