# 개발 일지 — 2026-04-04 (Day 2, Session 1)

## 이번 세션 작업 내역

### 전체 코드 리뷰 및 미구현 부분 식별
- 13개 서비스 전체 소스코드 정독
- 미구현/버그 3건 발견 및 수정 완료

### 버그 수정 (1건)
- [x] `NotificationType.PAYMENT_FAILED` 누락 — `EventConsumers`에서 결제 실패 알림에 `PAYMENT_COMPLETED` 타입을 잘못 사용하던 복붙 버그 수정
  - `NotificationType` enum에 `PAYMENT_FAILED` 추가
  - `EventConsumers.kt` PaymentFailedEvent 핸들러에서 타입 수정

### 미구현 기능 구현 (1건)
- [x] 트랜스코딩 파이프라인 완성 — `VideoUploadConsumer`가 job만 생성하고 실제 트랜스코딩을 실행하지 않던 문제 해결
  - `media-service`에 `S3StorageClient` 신규 생성 (S3 다운로드/업로드 지원)
  - `TranscodingService.processJob()` 구현: S3 다운로드 → FFmpeg 트랜스코딩 → S3 업로드 → job 상태 업데이트
  - `VideoUploadConsumer`에서 `TranscodingService.processJob()` 호출하도록 수정
  - `FFmpegTranscoder`의 `@Async` 제거 (`processJob()`이 이미 `@Async`이므로 중복 방지)

### 코드 정리 (1건)
- [x] 채팅 알림 TODO 주석 정리 — GPS 기반 채팅방은 고정 멤버가 없어 개별 알림 불가능하므로 의도 명시 주석으로 대체

### 빌드 상태
- 백엔드 13개 모듈: **전체 빌드 성공** (`compileKotlin` 통과)

---

## 변경 파일 목록
| 파일 | 변경 내용 |
|---|---|
| `notification-service/.../Notification.kt` | `PAYMENT_FAILED` enum 추가 |
| `notification-service/.../EventConsumers.kt` | PaymentFailed 타입 수정 + 채팅 TODO 정리 |
| `media-service/.../storage/S3StorageClient.kt` | 신규 — S3 다운로드/업로드 클라이언트 |
| `media-service/.../TranscodingService.kt` | `processJob()` 전체 파이프라인 구현 |
| `media-service/.../VideoUploadConsumer.kt` | TranscodingService 위임으로 리팩터링 |
| `media-service/.../FFmpegTranscoder.kt` | `@Async` 제거 (중복 방지) |

---

## 외부 API Key 설정 필요 목록
| 환경변수 | 용도 |
|---|---|
| `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` | Google OAuth2 로그인 |
| `KAKAO_CLIENT_ID` / `KAKAO_CLIENT_SECRET` | Kakao OAuth2 로그인 |
| `TOUR_API_KEY` | 한국관광공사 Tour API (숙소 검색) |
| `PORTONE_API_SECRET` / `PORTONE_STORE_ID` | PortOne 결제 검증 |
| `AES_SECRET_KEY` | 개인정보 암호화 키 (선택) |
