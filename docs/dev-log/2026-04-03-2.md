# 개발 일지 — 2026-04-03 (Day 1, Session 2)

## 이번 세션 작업 내역

### Tier 1: 실행 에러 수정
- [x] `/api/users/me` 엔드포인트 추가 (UserController)
- [x] eureka-server / api-gateway JPA 충돌 해결 (`excludeName` 방식으로 DataSource, Hibernate, Kafka 자동설정 제외)
- [x] 기존 4개 서비스 DB URL에 `createDatabaseIfNotExist=true` 추가
- [x] MinIO 버킷 자동 생성 (`@PostConstruct initBucket()`)

### Tier 2: 핵심 기능 동작
- [x] OAuth2 로그인 버튼 → 구글/카카오 redirect URL 구현 (프론트)
- [x] 포트원 SDK 프론트 연동 (`usePortone.ts` 훅 + iamport.js 동적 로드)
- [x] 채팅 WebSocket STOMP 연결 (`@stomp/stompjs` + `sockjs-client`)
- [x] HLS 비디오 플레이어 (`hls.js` + `HlsPlayer` 컴포넌트)
- [x] FFmpeg 트랜스코딩 Kafka Consumer (`VideoUploadConsumer`)
- [x] 프론트 `.env` 파일 생성 (Google/Kakao/PortOne 키 placeholder)

### 빌드 상태
- 백엔드 13개 모듈: **전체 빌드 성공**
- 프론트엔드: **빌드 성공**

---

## 남은 작업 (Tier 3)

### 아직 설정만 있고 실제 사용 안 하는 것
- [ ] QueryDSL 실제 쿼리 작성 (복잡한 동적 쿼리 필요한 곳에 적용)
- [ ] MyBatis 매퍼 XML 작성 (네이티브 SQL 필요한 곳에 적용)
- [ ] 나머지 서비스 간 gRPC 확장 (현재 booking→user/trip만)
- [ ] WebRTC 영상통화 구현
- [ ] AES-256 민감정보 암호화 (이메일, 이름 등)
- [ ] Dockerfile + docker-compose 서비스 빌드
- [ ] TDD 테스트 작성 (보류 중)

### 프론트엔드 미완성 페이지
- [ ] TripDetailPage, BookingsPage MUI+ReactQuery 전환 (일부 구형 패턴 남아있음)
- [ ] 결제 내역 페이지 실제 API 연동

---

## 현재 전체 실행 순서

```
1. docker-compose up -d
2. EurekaServerApplication       (:8761)
3. UserApplication                (:8081)
4. TripApplication                (:8082)
5. BookingApplication             (:8083)
6. PaymentApplication             (:8084)
7. AccommodationApplication       (:8085)
8. SubscriptionApplication        (:8086)
9. FeedApplication                (:8087)
10. MediaApplication              (:8088)
11. ChatApplication               (:8089)
12. NotificationApplication       (:8090)
13. ApiGatewayApplication         (:8080)
14. cd frontend && npm run dev    (:5173)
```
